{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run Extension",
            "type": "extensionHost",
            "request": "launch",
            "args": [
                "--extensionDevelopmentPath=${workspaceFolder}/vscode-calcpad"
            ],
            "outFiles": [
                "${workspaceFolder}/vscode-calcpad/out/**/*.js"
            ],
            "preLaunchTask": "prep-extension"
        }
    ]
}
