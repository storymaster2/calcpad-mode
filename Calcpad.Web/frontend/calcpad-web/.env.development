# Default remote engine for local `npm run dev` testing (base stays `/`).
# Cloud builds use VITE_BASE_PATH=/calcpad/ via Dockerfile / cloudbuild.web.yaml.
# Override anytime with ?server=... or Settings → Remote Server URL.
VITE_SERVER_URL=https://calcpad-server-914029425445.us-west1.run.app
